export const environment = {
  production: false,
  // Update this URL to match your backend server
  // Make sure your backend server is running on this port
  apiURL: 'http://localhost:5065/api',
  // If your backend is running on a different port, update it here
  // Common ports: 5000, 5065, 8080, 3000
};
